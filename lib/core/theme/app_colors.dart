import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1E2875);
  static const secondary = Color(0xFF00D9FF);
  static const white = Colors.white;
  static const text = Color(0xFF1E1E1E);
}
